<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.power-extension-manager" path="/org/gnome/shell/extensions/power-extension-manager/">
    <key name="profile-rules" type="a{sas}">
      <default>{}</default>
      <summary>Extension enablement rules by power profile</summary>
      <description>
        A map from extension UUID to the list of power profiles where that extension should be enabled.
        Extensions absent from the map are not managed.
      </description>
    </key>
  </schema>
</schemalist>
